kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
data:
  DB_TYPE: postgres
  DB_HOST: postgresql.core-services.svc
  DB_PORT: 5432
  DB_NAME: wiki