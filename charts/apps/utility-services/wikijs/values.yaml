image: ghcr.io/requarks/wiki:2.5.308
namespace: utility-services

host:
  subdomain: wiki

secrets:
  domain:
    key: homeDomain
    path: kv/data/domains

service:
  type: ClusterIP
  ports:
  - name: http
    containerPort: 80
    externalPort: 8081
    protocol: TCP
  
