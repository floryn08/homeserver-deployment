grafana:
  namespaceOverride: core-services
  service:
    type: LoadBalancer
    port: 8100
  datasources: |
    datasources.yaml:
      apiVersion: 1
      datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-server.core-services.svc
        access: proxy
        isDefault: true