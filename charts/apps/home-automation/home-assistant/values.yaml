namespace: home-automation

# External Home Assistant running on mini PC
external:
  # IP address of the mini PC running Home Assistant
  ip: "192.168.0.15"  # UPDATE THIS to your mini PC's IP
  port: 8123

host:
  subdomain: homeassistant

secrets:
  primaryDomain:
    key: primaryDomain
    path: kv/data/domains
  homeDomain:
    key: homeDomain
    path: kv/data/domains

