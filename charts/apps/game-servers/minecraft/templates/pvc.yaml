apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
  annotations:
    longhorn.io/volume-name: "{{ .Release.Name }}" 
    longhorn.io/numberOfReplicas: "1"  # Set replica count for this volume only
    longhorn.io/diskSelector: "ssd"  # Assign to disks with this tag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.longhorn.size }}
  storageClassName: longhorn