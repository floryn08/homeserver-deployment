apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  entryPoints:
    - websecure
  routes: 
    - kind: Rule
      match: Host(`minecraft.{{ .Values.domain }}`) && PathPrefix(`/`)
      middlewares: 
        - name: bouncer
          namespace: core-services
      services: 
        - kind: Service
          name: {{ .Release.Name }}-mc-router
          namespace: {{ .Values.namespace }} 
          port: proxy
  tls:
    certResolver: letsencrypt