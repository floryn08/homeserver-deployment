apiVersion: longhorn.io/v1beta1
kind: Volume
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  size: {{ .Values.persistence.volume.size | quote}}
  numberOfReplicas: {{ .Values.persistence.volume.replicas }}
