apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  ports:
  - port: 8989
    targetPort: {{ .Release.Name }}
    name: {{ .Release.Name }}
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}
