kind: PersistentVolume
apiVersion: v1
metadata:
  name: appdata-pv
  labels:
    type: appdata
spec:
  storageClassName: manual
  capacity:
    storage: 700Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.appdataVolumePath }}
    type: DirectoryOrCreate