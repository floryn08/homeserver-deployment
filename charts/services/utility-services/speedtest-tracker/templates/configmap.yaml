kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-configmap
  namespace: {{ .Release.Namespace }}
data:
  PUID: "1000"
  PGID: "1000"
  DB_CONNECTION: pgsql
  DB_HOST: postgres-loadbalancer.core-services.svc
  DB_PORT: "5432"
  DB_DATABASE: speedtest-tracker
  DB_USERNAME: user
  DB_PASSWORD: parola123