apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: vaultstaticsecret-mysecret
  namespace: {{ .Release.Namespace }}
spec:
  # vaultAuthRef: vaultauth-sample
  mount: kv
  type: kv
  path: secrets
  refreshAfter: 5s
  destination:
    name: test