apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ui-service
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  ports:
    - name: http
      nodePort: 8080
      targetPort: 80
      protocol: TCP
  selector:
    app: {{ .Release.Name }}
