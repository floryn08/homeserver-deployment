apiVersion: v1
kind: Service
metadata:
  name: sonarr-loadbalancer
  namespace: media-management
spec:
  type: LoadBalancer
  ports:
  - port: 8099
    targetPort: 8089
    protocol: TCP
    name: sonarr-port
  selector:
    app: sonarr
