apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-loadbalancer
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  ports:
  - port: 8989
    targetPort: 8989
    name: sonarr-port
  selector:
    app: sonarr
