apiVersion: v1
kind: Service
metadata:
  name: sonarr-loadbalancer
  namespace: media-management
spec:
  type: LoadBalancer
  ports:
  - port: 8990
    targetPort: 8989
    protocol: TCP
    name: sonarr-port
  selector:
    app: sonarr
