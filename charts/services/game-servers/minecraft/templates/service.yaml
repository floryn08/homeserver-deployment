apiVersion: v1
kind: Service
metadata:
  name: minecraft-service
  namespace: game-servers
spec:
  type: LoadBalancer
  ports:
  - name: minecraft-port
    port: 20281
    targetPort: minecraft-port
  selector:
    app: minecraft
