apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sonarr
spec:
  project: server
  destination:
    server: https://kubernetes.default.svc
    namespace: server
  source:
    repoURL: https://github.com/floryn08/homeserver-deployment
    path: charts/services/sonarr
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - RespectIgnoreDifferences=true
    retry:
      limit: 15
      backoff:
        duration: 10s
        factor: 3
        maxDuration: 3m0s