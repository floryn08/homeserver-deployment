# TODO
name: Install master-app in ArgoCD

on:
  workflow_dispatch: 

jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.6
        
      - name: Set up Kubectl
        uses: azure/k8s-set-context@v4.0.0
        with:
          kubeconfig: ${{ secrets.KUBECONFIG }}